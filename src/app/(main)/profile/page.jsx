import ProfileClient from "@/Components/ProfileClient/ProfileClient"

const Profile = () => {
    return (
        <div>
            <ProfileClient/>
        </div>
    );
};

export default Profile;